process_manager:
  port: 50055

managed_processes:
  # Simple integrated process (enabled field omitted - defaults to true)
  - id: "test-integrated"
    type: "integrated_managed"
    profile_type: "web"  # ✅ NEW: Echo gRPC server is a web service
    unit:
      integrated_managed:
        metadata:
          name: "Echo gRPC server as integrated unit"
        control:
          execution:
            executable_path: "..\\..\\..\\..\\hsu-example1-go\\cmd\\srv\\echogrpcsrv\\echogrpcsrv.exe"
